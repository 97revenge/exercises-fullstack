import * as flowing from "../__exercises__";

export default Object.prototype.constructor = new Object({
  whileFirst: flowing.default[0][0]["while"][0]["name"],
  whileSecond: flowing.default[0][0]["while"][1]["name"],
  whileThird: flowing.default[0][0]["while"][2]["name"],
});
